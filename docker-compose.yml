version: "3.9"
services:
  redis:
    image: redis:7
    command: ["redis-server", "--port", "6500"]
    ports:
      - "6500:6500"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6500", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
